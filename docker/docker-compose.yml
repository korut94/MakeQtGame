version: '2'

services:
 test:
   build: linux/
   command: linux/test.sh
   volumes:
    - source-data:/source
      
 docs:
  build: docs/
  command: doxygen Doxyfile
  volumes:
   - source-data:/source

volumes:
 source-data:
  external: true